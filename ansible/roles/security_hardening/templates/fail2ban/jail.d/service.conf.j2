# Fail2Ban jail configuration for {{ item.name }}
# Generated by Ansible - Do not edit manually

[{{ item.name }}]
enabled = {{ item.enabled | lower }}
port = {{ item.port }}
filter = {{ item.name }}
logpath = %({{ item.name }}_log)s
maxretry = {{ item.maxretry | default(fail2ban_maxretry) }}
bantime = {{ item.bantime | default(fail2ban_bantime) }}
findtime = {{ item.findtime | default(fail2ban_findtime) }}