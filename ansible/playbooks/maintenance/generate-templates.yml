---
- name: Generate Semaphore Templates
  hosts: localhost
  connection: local
  gather_facts: no
  
  tasks:
    - name: Run template generation script
      command: python3 tools/generate-templates.py
      register: generate_result
      
    - name: Display generation results
      debug:
        var: generate_result.stdout_lines